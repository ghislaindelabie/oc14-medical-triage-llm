# Data — raw/interim/processed are NOT committed (size + licensing). Provenance cards ARE (see !data/cards).
data/*
!data/cards/
!data/cards/**
# Models / weights / checkpoints
models/
*.gguf
*.safetensors
checkpoints/
# Python
.venv/
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
*.egg-info/
# Experiment tracking / logs
wandb/
*.log
# Secrets — never commit
.env
.env.*
*secret*
kaggle.json
# OS / editor
.DS_Store
.ipynb_checkpoints/
