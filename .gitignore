# Data — raw/interim/processed are NOT committed (size). The FINAL shipped datasets
# (open-licence sources + own synthetic — safe to publish) + provenance cards ARE committed.
data/*
!data/cards/
!data/cards/**
!data/kaggle_upload/
!data/kaggle_upload/**
!data/kaggle_upload_v10/
!data/kaggle_upload_v10/**
# Derived / runtime artefacts (not versioned)
submission/
.gradio/
# Models / weights / checkpoints
models/
*.gguf
*.safetensors
checkpoints/
# Python
.venv/
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
*.egg-info/
# Experiment tracking / logs
wandb/
*.log
# Secrets — never commit
.env
.env.*
*secret*
kaggle.json
# OS / editor
.DS_Store
.ipynb_checkpoints/
