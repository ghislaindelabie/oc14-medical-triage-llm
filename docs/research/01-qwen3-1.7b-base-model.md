> **TL;DR — key takeaways**
>
> - Qwen3-1.7B-Base is a 1.7B-parameter (1.4B non-embedding) causal language model released April 2025 under Apache 2.0, making it freely usable for both research and commercial medical applications without royalty obligations.
> - The Base variant is a raw next-token predictor with no instruction following — it deliberately starts here for SFT so the fine-tuning process owns the full prompt format and alignment signal, avoiding conflicts with an existing instruct persona.
> - VRAM footprint at BF16/FP16 is roughly 3.4 GB for weights alone (confirmed by official Qwen benchmark: 3,412 MB at 1-token context), rising to ~7 GB at full 32k context due to KV cache; 4-bit QLoRA training on a T4 16 GB is feasible and has been demonstrated in practice.
> - The Qwen3 tokenizer ships a full ChatML Jinja2 template (using <|im_start|>/<|im_end|> special tokens) even on the Base checkpoint, so you do not need to write a template from scratch — you adopt it as-is for SFT and must set eos_token='<|im_end|>' in training config.
> - Thinking mode (chain-of-thought inside <think>...</think> blocks) is a post-training feature of the Instruct variant and is not natively activated by the Base model; after your own SFT you should disable it (enable_thinking=False) for a triage assistant to reduce latency and token count.
> - Known LoRA fine-tuning quirks: do not use greedy decoding after training (causes repetition loops), mask loss on non-assistant turns, pin transformers>=4.51.0 to avoid a KeyError on 'qwen3', and keep LoRA rank modest (r=16–32) given the small parameter budget.


# Qwen3-1.7B-Base: Reference Guide for the CHSA Medical Triage Project

> This document covers everything a learner needs to know about the base model before writing a single line of training code. Each section defines terms on first use.

---

## 1. What Qwen3 Is

**Qwen3** is the third generation of Alibaba Cloud's open-weight large language model family, released on 28 April 2025. "Open-weight" means the trained parameter values are publicly downloadable, unlike proprietary models such as GPT-4. The family spans both *dense* models (every parameter is used for every token) ranging from 0.6 B to 32 B parameters, and *Mixture-of-Experts* (MoE) models (only a subset of parameters activates per token) at 30 B-A3B and 235 B-A22B scales.

All Qwen3 models are released under the **Apache 2.0 license** ([Qwen3 blog](https://qwenlm.github.io/blog/qwen3/)), which is a permissive open-source license. Concretely this means:
- Commercial use is allowed without paying royalties.
- Medical-research use is allowed.
- You may redistribute modified versions.
- You must keep the copyright notice in any redistribution.

There is no "research-only" clause, no country restriction, and no size-based commercial threshold (unlike some Meta Llama licenses). For this POC, Apache 2.0 is straightforwardly compatible.

**Official HuggingFace page:** [Qwen/Qwen3-1.7B-Base](https://huggingface.co/Qwen/Qwen3-1.7B-Base)

---

## 2. The 1.7B-Base Variant: Specifications

| Property | Value | Source |
|---|---|---|
| **HuggingFace ID** | `Qwen/Qwen3-1.7B-Base` | HF model card |
| **Total parameters** | 1.7 B | HF model card |
| **Non-embedding parameters** | 1.4 B | HF model card |
| **Architecture type** | Dense causal transformer | HF model card |
| **Transformer layers** | 28 | HF model card |
| **Attention heads (query)** | 16 | HF model card |
| **Attention heads (key/value)** | 8 (GQA) | HF model card |
| **Context window** | 32,768 tokens (32k) | HF model card |
| **Native data type** | BF16 | HF model card |
| **Vocabulary size** | 151,669 tokens | Qwen search results |
| **Tokenizer type** | Byte-level BPE | Qwen documentation |
| **Pre-training data** | 36 trillion tokens, 119 languages | Qwen3 Technical Report ([arXiv:2505.09388](https://arxiv.org/abs/2505.09388)) |
| **License** | Apache 2.0 | HF model card |
| **Min. transformers version** | >= 4.51.0 | HF model card |

**GQA** (Grouped Query Attention) means multiple query heads share a single key/value head, which reduces the KV cache memory footprint during inference compared to standard multi-head attention.

**Pre-training was three-staged:** (1) broad language acquisition on general web data, (2) focused reasoning — STEM, code, mathematics, (3) extended-context training up to 32k tokens. The architecture uses SwiGLU activations, RoPE positional embeddings, and RMSNorm — all standard modern choices that are well-supported by fine-tuning frameworks.

---

## 3. Base Model vs. Instruct/Chat Variant

### What a "Base" model is

A *base model* (also called a *pre-trained model* or *foundation model*) is the direct output of pre-training: it has learned to predict the next token from a vast corpus, but it has **no notion of instruction following**. If you send it "Translate to French: hello", it will continue the sentence statistically — it might write more English text, or attempt a translation, but it has no reliable guarantee of doing the right thing.

### What an "Instruct" or "Chat" model is

An *instruct model* is a base model that has been further trained on examples of instructions and responses (a process called *Supervised Fine-Tuning*, or SFT), followed typically by preference alignment (RLHF, DPO, or GRPO). This teaches the model to follow a specific conversation format, obey a system prompt, and respond helpfully.

**Qwen3 naming convention** (note: different from Llama):
- `Qwen/Qwen3-1.7B-Base` → raw pre-trained model, no alignment
- `Qwen/Qwen3-1.7B` → post-trained (SFT + RL), includes thinking mode

### Why a fine-tuning project deliberately starts from Base

When you fine-tune an instruct model, you are working on top of alignment that was baked in by Alibaba. That alignment includes:
1. A specific system prompt style and persona ("I am Qwen, created by Alibaba Cloud").
2. Refusal behaviors and safety filters that may conflict with medical Q&A.
3. A thinking mode trained to activate on reasoning-heavy prompts.

Starting from Base means:
- **You own the entire prompt format.** You choose the chat template, the system prompt content, and the expected response style. There are no competing priors.
- **Your SFT signal is cleaner.** The model has not been told "think before answering" — you decide whether to include or exclude thinking tokens in training labels.
- **Catastrophic forgetting risk is lower.** Instruct models have already been SFT'd once; running another SFT on them risks overwriting alignment in unpredictable ways.
- **It matches real production pipelines.** Academic and industrial fine-tuning papers almost universally start from Base for domain-specific adaptation.

The trade-off is that a Base model **cannot be used directly** as a chat assistant out of the box — it requires your SFT to become usable.

---

## 4. VRAM Footprint and GPU Feasibility

### How to calculate weights-only VRAM

The rule of thumb is: **N billion parameters × 2 bytes per parameter = X GB in FP16/BF16**. For 1.7 B parameters: 1.7 × 2 = **3.4 GB** just for weights. At 4-bit quantization (0.5 bytes/param): approximately **0.85–1.1 GB** for weights.

The **official Qwen benchmark** ([speed_benchmark](https://qwen.readthedocs.io/en/latest/getting_started/speed_benchmark.html)) measured on a single H20 GPU confirms:

| Precision | Context length | GPU memory (MB) |
|---|---|---|
| BF16 | 1 token | 3,412 |
| FP8 | 1 token | 2,726 |
| GPTQ-Int8 | 1 token | 2,229 |
| BF16 | 6,144 tokens | 4,213 |
| BF16 | 14,336 tokens | 5,109 |
| BF16 | 30,720 tokens | 6,902 |

The increase from 1 to 30k tokens reflects the **KV cache** (Key-Value cache) growing linearly with context length. The KV cache stores intermediate attention computations so the model does not recompute them on every new token.

### (a) LoRA fine-tuning on a free T4 (16 GB)

Fine-tuning requires more memory than inference because the GPU must also hold:
- Gradients (same size as weights)
- Optimizer states (e.g., Adam stores two running averages per parameter = 2× weights in FP32)
- Activation checkpoints

Full fine-tuning of 1.7B in BF16 would require roughly **3.4 GB (weights) + 3.4 GB (gradients) + 6.8 GB (Adam states) ≈ 14 GB** — borderline on a 16 GB T4.

**QLoRA** (Quantized LoRA) is the standard solution. It:
1. Loads the frozen base model in 4-bit (NF4) format: ~1.1 GB.
2. Trains small low-rank adapter matrices (LoRA) in BF16: negligible extra VRAM for small rank.
3. Eliminates full optimizer states for the frozen weights.

A confirmed real-world run fine-tuned Qwen3-1.7B with QLoRA on a **free Google Colab T4 (15 GB VRAM)** in roughly 10–15 minutes on 1,000 examples ([HomeDock blog](https://www.homedock.cloud/blog/self-hosting/how-we-fine-tuned-a-1-7b-llm-to-talk-like-a-ghost/)). Configuration: r=16, lora_alpha=32, target modules: q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj.

For the 5,000-sample SFT in this project, a typical Colab or Kaggle T4 session should be sufficient with:
- Batch size 1 per device + gradient accumulation steps 4–8 (effective batch 4–8)
- Sequence length capped at 512–1024 tokens per sample
- `gradient_checkpointing=True` (trades 30% speed for ~30% activation memory reduction)

**Recommended framework: [Unsloth](https://unsloth.ai/docs/models/qwen3-how-to-run-and-fine-tune)**, which reports 2× faster training and ~70% lower VRAM for Qwen3 models compared to vanilla HuggingFace + PEFT.

### (b) vLLM inference on a small GPU

For inference only (no training), the 1.7B model at BF16 needs ~3.4 GB for weights plus KV cache proportional to context length. On a T4 16 GB, this leaves >12 GB for KV cache, which is more than enough to serve long medical conversations. Even a 2 GB serverless GPU (e.g., RunPod smallest tier) could serve the model in 4-bit GPTQ or AWQ format.

**Important vLLM note:** vLLM reserves memory aggressively by default (`gpu_memory_utilization=0.9`). For a 1.7B model you should lower this to avoid wasting capacity, or explicitly set `MAX_MODEL_LEN` to the maximum context you actually need (e.g., 2048 for triage Q&A).

---

## 5. Tokenizer and Chat Template

### Tokenizer

Qwen3 uses a **byte-level Byte-Pair Encoding (BPE)** tokenizer with a vocabulary of 151,669 tokens. "Byte-level" means it can tokenize any Unicode string without unknown-token failures, which is important for bilingual French/English text with accents and medical abbreviations.

### The ChatML chat template

Even the **Base** checkpoint ships with a Jinja2 chat template in its `tokenizer_config.json`. This template uses the **ChatML format** — the same format used by OpenAI's GPT models and many open-source models. The special tokens are:

```
<|im_start|>system
{system prompt text}
<|im_end|>
<|im_start|>user
{user message}
<|im_end|>
<|im_start|>assistant
<think>
{chain-of-thought, may be empty}
</think>
{final response}
<|im_end|>
```

Key special tokens and their IDs:
- `<|im_start|>` — marks the beginning of a role turn (ID: in vocabulary)
- `<|im_end|>` — marks the end of a role turn (ID: 151645); used as EOS token
- `</think>` — marks the end of a thinking block (ID: 151668)
- `<think>` / `</think>` — reasoning block delimiters; tokenized as regular text, not special tokens

**Critical fine-tuning config:** You must set `eos_token="<|im_end|>"` in your `SFTConfig` or `TrainingArguments`, otherwise the trainer uses a different EOS and the model never learns to stop generating.

The template is invoked via:

```python
text = tokenizer.apply_chat_template(
    messages,          # list of {"role": ..., "content": ...} dicts
    tokenize=False,
    add_generation_prompt=True,
    enable_thinking=False   # see section 6
)
```

**Does the Base model "know" ChatML?** The Base model was pre-trained on data that includes ChatML-formatted text, so the special tokens `<|im_start|>` and `<|im_end|>` are in its vocabulary and have non-random embeddings. Your SFT will reinforce their meaning rather than teaching from zero — a significant advantage over models whose templates use tokens absent from pre-training.

### Template consistency rule

Whatever template you use in SFT training **must be used identically at inference time**. This means:
- Use the same Jinja template at training and serving.
- In vLLM, the template is read from the model's `tokenizer_config.json` automatically. Do not override it with `--chat-template chatml` (the generic ChatML template lacks the `enable_thinking` logic and will silently break thinking-mode suppression).

---

## 6. Thinking Mode: Relevant or Not for a Triage Assistant?

### What thinking mode is

Thinking mode is a post-training feature introduced in Qwen3 Instruct models. When enabled, the model generates a step-by-step reasoning trace inside `<think>...</think>` tags before the final answer. This improves performance on math, coding, and multi-step logic tasks at the cost of higher latency and more tokens.

The mode is controlled via:
- `enable_thinking=True/False` in `tokenizer.apply_chat_template()` at inference time.
- For vLLM: `--reasoning-parser qwen3 --default-chat-template-kwargs '{"enable_thinking": false}'`
- Request-level override: `extra_body={"chat_template_kwargs": {"enable_thinking": false}}`

When `enable_thinking=False`, the template inserts a prefilled `<think>\n\n</think>` block, instructing the model to skip the reasoning step and respond directly.

### Is it relevant for this project?

**For the Base → SFT pipeline specifically:**

The Base model has no thinking behavior at all — thinking is an instruct-training artifact. During your SFT, you will define what the model learns. The simplest and most recommended approach for this POC is:

1. **Exclude `<think>` blocks from SFT training labels entirely.** Your training examples should follow the format: system prompt → user turn → direct assistant answer (no `<think>` section).
2. After SFT, serve with `enable_thinking=False` to prevent the template from injecting thinking prompts.

**Why thinking mode is not useful for medical triage:**
- A triage assistant needs **fast, direct answers** (urgency classification, next action). Latency matters.
- Adding chain-of-thought from a 1.7B model adds token cost without reliability — small models often produce hallucinated or circular reasoning in `<think>` blocks.
- Medical safety risk: a visible reasoning trace could mislead clinicians if the reasoning is wrong but the final answer is right.

**Known vLLM bug:** On vLLM versions < 0.9.0, `enable_thinking=False` was sometimes ignored, causing the model to still emit `<think>` tokens. Pin vLLM >= 0.9.0 in your Docker image ([vLLM issue #35574](https://github.com/vllm-project/vllm/issues/35574)).

---

## 7. Known Quirks for Fine-Tuning Qwen3 Small Models

### 7.1 Transformers version pin

The model requires `transformers >= 4.51.0`. Older versions raise a `KeyError: 'qwen3'` when loading the model config. Pin this in your `requirements.txt`.

### 7.2 EOS token configuration

As noted above, always set `eos_token="<|im_end|>"` in `SFTConfig`. Without this, training will use a default EOS that does not match the chat template, and the model will not learn to stop at the right place.

### 7.3 Loss masking on non-assistant turns

During SFT, you should compute loss only on the **assistant's response tokens**, not on the system prompt or user turn. This is called "label masking." In TRL's `SFTTrainer`, use `dataset_text_field` with complete formatted strings and enable `packing=False`, or use a data collator that masks the prompt portion. Failing to mask leads the model to "memorize" the prompt format rather than learning to generate responses.

### 7.4 Avoid greedy decoding after training

Set temperature > 0 during inference (e.g., 0.6–0.7). Qwen3's official documentation warns explicitly: **do not use greedy decoding** (temperature=0), as it "can lead to performance degradation and endless repetitions" — this applies both in thinking and non-thinking mode. Use temperature=0.7, top-p=0.8, top-k=20 for a triage assistant.

### 7.5 Repetition loops at small model scale

1.7B models are more prone to repetition loops than larger models, especially on out-of-distribution inputs. Mitigation:
- `repetition_penalty=1.1–1.2` at inference time.
- Optionally `presence_penalty=1.5` if repetition persists.

### 7.6 Thinking tokens leaking after SFT

If your SFT dataset accidentally includes `<think>` or `</think>` tokens in any training example (e.g., copied from an instruct model's output), the model may start generating spurious thinking blocks. Filter your dataset for these strings before training.

### 7.7 LoRA target modules

For Qwen3's architecture, target all seven projection layers for best results:
```python
target_modules=["q_proj", "k_proj", "v_proj", "o_proj",
                 "gate_proj", "up_proj", "down_proj"]
```
Targeting only attention layers (q/k/v/o) works but leaves the MLP layers (gate/up/down) frozen, which is suboptimal for domain adaptation.

### 7.8 Bilingual data and language mixing

Qwen3 supports 119 languages including French and English, so bilingual SFT is well-supported. However, 1.7B models can exhibit language mixing (switching mid-response). Mitigate by including an explicit system prompt instruction: "Réponds toujours dans la même langue que la question." ("Always respond in the same language as the question.") and validate on a held-out bilingual test set early.

### 7.9 DPO after SFT

For the DPO preference-alignment step, use the fine-tuned SFT checkpoint (not the Base) as the reference model. The DPO training format must use the same ChatML template and the same EOS token as SFT.

---

## 8. Serving with vLLM

The recommended vLLM command for this project's API endpoint:

```bash
vllm serve <your-sft-model-path> \
    --dtype bfloat16 \
    --max-model-len 2048 \
    --gpu-memory-utilization 0.85 \
    --reasoning-parser qwen3 \
    --default-chat-template-kwargs '{"enable_thinking": false}'
```

Flags explained:
- `--dtype bfloat16`: matches the model's native format; saves memory vs float32.
- `--max-model-len 2048`: caps context to what triage Q&A actually needs; prevents KV cache from exhausting VRAM.
- `--reasoning-parser qwen3`: tells vLLM how to parse and separate any thinking tokens from the final response (required even when disabling thinking, so the framework handles the `<think>` tokens correctly).
- `--default-chat-template-kwargs`: disables thinking server-wide; individual requests can override with `extra_body`.

For RunPod serverless deployment, pass these as environment variables via the RunPod worker's `MODEL_ARGS` or equivalent env var per the [RunPod vLLM docs](https://docs.runpod.io/serverless/vllm/get-started).

---

## 9. Summary Table: Decision Points

| Decision | Recommendation | Rationale |
|---|---|---|
| Base or Instruct for SFT? | **Base** | Own the full format; no conflicting alignment |
| Chat template | **Qwen3 ChatML (ship with model)** | Already in vocab; no template from scratch needed |
| Thinking mode in SFT labels? | **Exclude** | Reduces latency; 1.7B thinking is unreliable |
| Thinking mode at inference? | **Disable** (`enable_thinking=False`) | Speed + safety for triage |
| Precision for training? | **4-bit QLoRA (NF4 + BF16 compute)** | Fits T4 16 GB comfortably |
| LoRA rank | **r=16** (up to r=32) | Proven sufficient for 1k–5k samples |
| Training framework | **Unsloth + TRL** | 2× speed, 70% less VRAM on Qwen3 |
| vLLM version | **>= 0.9.0** | `enable_thinking=False` reliable only from this version |

---

## Sources

- [Qwen/Qwen3-1.7B-Base — HuggingFace model card](https://huggingface.co/Qwen/Qwen3-1.7B-Base)
- [Qwen/Qwen3-1.7B — HuggingFace model card (instruct variant)](https://huggingface.co/Qwen/Qwen3-1.7B)
- [Qwen3: Think Deeper, Act Faster — Official Qwen Blog](https://qwenlm.github.io/blog/qwen3/)
- [Qwen3 Technical Report — arXiv:2505.09388](https://arxiv.org/abs/2505.09388)
- [Qwen Quickstart — Read the Docs](https://qwen.readthedocs.io/en/latest/getting_started/quickstart.html)
- [Qwen Speed Benchmark (official VRAM figures)](https://qwen.readthedocs.io/en/latest/getting_started/speed_benchmark.html)
- [The 4 Things Qwen-3's Chat Template Teaches Us — HuggingFace Blog](https://huggingface.co/blog/qwen-3-chat-template-deep-dive)
- [Fine-Tuning Qwen3-1.7B with LoRA — HomeDock Cloud](https://www.homedock.cloud/blog/self-hosting/how-we-fine-tuned-a-1-7b-llm-to-talk-like-a-ghost/)
- [Unsloth Qwen3 Fine-Tuning Documentation](https://docs.unsloth.ai/models/qwen3-how-to-run-and-fine-tune)
- [vLLM Reasoning Outputs Documentation](https://docs.vllm.ai/en/latest/features/reasoning_outputs/)
- [vLLM bug: enable_thinking=False ignored — Issue #35574](https://github.com/vllm-project/vllm/issues/35574)
- [Guide to Deploying Qwen3 with vLLM on RunPod](https://medium.com/@mshojaei77/guide-to-deploying-qwen-3-with-vllm-on-runpod-31b9da6642d0)
- [Fine-Tuning Qwen3 — DataCamp Tutorial](https://www.datacamp.com/tutorial/fine-tuning-qwen3)
- [Comprehensive Hardware Requirements for Qwen3 — DEV Community](https://dev.to/ai4b/comprehensive-hardware-requirements-report-for-qwen3-part-ii-4i5l)


---

## Open questions to confirm during implementation

- Does the Qwen3-1.7B-Base tokenizer_config.json ship an identical chat_template jinja to the Instruct variant, or a stripped version? Worth verifying by diffing both files on HuggingFace before starting SFT.
- Exact peak VRAM during QLoRA training (not just inference) on a T4 with sequence length 512–1024 and batch size 1 + gradient accumulation has not been directly benchmarked in public sources for the 1.7B size — run a quick memory profiling pass before committing to the full 5k-sample SFT run.
- The vLLM --reasoning-parser qwen3 flag and enable_thinking=False were reported buggy on vLLM < 0.9.0 for some Qwen3.x variants; confirm the vLLM version available on your RunPod worker image supports this flag cleanly before deploying.
- Whether to train WITH <think> blocks in SFT labels (to produce a thinking-capable model that you then suppress at inference) or WITHOUT them entirely (simpler, less risk of thinking bleed) depends on your preference for latency vs. reasoning depth — the right choice for this POC is to exclude them entirely for simplicity, but confirm with the grader rubric.
- Bilingual (FR+EN) SFT may cause language mixing in outputs for small models — Qwen3 multilingual support covers both languages, but you should add an explicit French-or-English instruction in the system prompt and test for language consistency early.
